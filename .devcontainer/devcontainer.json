{
  "name": "Codespace with Docker",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "docker-in-docker": "latest"
  },
  "postCreateCommand": "docker --version && docker run hello-world"
}

  